<x-page title="Metrics">

<x-p>
    These blocks are intended for displaying statistics
</x-p>

<x-p>
    You can show them on the <x-link link="{{ route('moonshine.page', 'advanced-dashboard') }}">admin panel</x-link> home page
    and in every individual <x-link link="{{ route('moonshine.page', 'resources-index') }}">resource</x-link> as well
</x-p>

<x-p>
    Metrics are also provided with
    <x-link link="{{ route('moonshine.page', 'decorations-layout#grid-column') }}">Layout (columnSpan)</x-link> decorator features
</x-p>
</x-page>
